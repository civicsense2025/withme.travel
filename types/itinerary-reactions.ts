/**
 * Type definition for itinerary item reactions (emoji reactions)
 */
export interface ItineraryItemReaction {
  id: string;
  item_id: string;
  user_id: string;
  emoji: string;
  created_at: string;
}
