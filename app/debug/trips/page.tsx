import TripDebugDashboard from './trip-debug-dashboard';

export const metadata = {
  title: 'Trip Debug Tool - WithMe.Travel',
  description: 'Diagnostic tools for debugging trip data loading issues',
};

export default function TripDebugPage() {
  return <TripDebugDashboard />;
}

