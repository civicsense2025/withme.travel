/**
 * Re-export the group members hook from the main hook location
 * This maintains the feature-first organization while preserving
 * compatibility with existing code
 */
export * from '../../../hooks/use-group-members'; 